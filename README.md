# simple-ethereumjs-wallet-gen
Simple utility for whenever you need a wallet on the fly

**Steps to use**

install packages with `npm install`

create a .env file. See .sample-env for reference

run with `node keystoregen.js`
